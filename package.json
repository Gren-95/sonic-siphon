{
  "name": "yt2mp3docker",
  "version": "1.0.0",
  "description": "YouTube to MP3 downloader with Tailwind CSS",
  "scripts": {
    "build:css": "tailwindcss -i ./static/src/input.css -o ./static/css/main.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}

